# Student 2 — Progress Summary

This document summarizes the work performed by Student 2 (branch: Student2-Matej).

## Overview
- Project: TuliPani (Spring Boot + JPA)
- Focus: implement Student 2 service functionality, unit tests, repository helpers, audit logging, and align JPA entities to provided Supabase schema.
- Branch: Student2-Matej

## Key Commits (examples)
- Implement TaskService: createTask & getTask
- implement todolist and task remaining methods
- Tidy JPA: cascade tasks on Todolist
- Tidy JPA: align updatedBy, audit times, and nullability
- Tidy JPA: use LocalDateTime for deadlines and DTOs; relax name/state nullability
- Tidy JPA: relax comment.text nullability
- Fix UpdateTaskStatusRequest accessor: use state()

## Services Implemented / Updated
- `TodolistService`
  - createTodolist, listTodolists, getTodolist
  - addUserToTodolist, removeUserFromTodolist
  - deleteTodolist (permission checks + audit logging)

- `TaskService`
  - createTask (validates category/todolist, sets `taskCreator`, `updatedBy`, audit log)
  - getTask
  - assignUserToTask / unassignUserFromTask (membership checks, role checks, `TaskUser` management, audit)
  - updateTaskStatus (permission checks by creator/member/role, status validation, audit)
  - deleteTask (removes `TaskUser` entries, deletes `Task`, audit)

## Repositories Extended
- `TaskUserRepository`: `existsByTaskIdAndUserId`, `findByTaskIdAndUserId`, `findByTaskId`
- `TodolistUserRepository`: `existsByTodolistIdAndUserId`, `findByTodolistIdAndUserId`, `findByUserId`
- `AppUserRepository`: `findByAuthId`

## Entities / JPA Alignment Changes
Aligned entities to `supabase_schema.sql` (types, nullability, field names):
- `Task`
  - `updatedBy` changed to `String` (maps to `updated_by text`)
  - `deadline` changed to `LocalDateTime` (timestamp without time zone)
  - relaxed `name` / `state` nullability
  - `@ManyToOne` associations for `todolist` and `category` (insertable=false/updatable=false)

- `Todolist`
  - cascade `tasks` with `orphanRemoval=true`
  - relaxed `name` nullability

- `TodolistUser`
  - `roleId` marked `nullable=false`

- `TaskComment`
  - `createdAt` -> `LocalDateTime`
  - relaxed nullability for `text`

- `AuditLog`
  - `logTime` -> `LocalDateTime`
  - relaxed action/logTime nullability

- `UserSettings`
  - `preferences` set to length 600 (varchar(600))

- `Attachment`, `Category`, `AppUser` — various nullability relaxations

## Tests Added / Updated
- `TaskServiceTest` — create/get/assign/unassign/update/delete success-path tests updated to match type changes (deadlines as `LocalDateTime`, `updatedBy` as `authId` string)
- `TodolistServiceTest` — existing tests retained/updated where needed

## Other Changes
- Fixed circular DI in `SecurityConfig` by injecting `JwtAuthenticationFilter` into the `securityFilterChain` method to avoid bean-creation cycles.
- Numerous small fixes (accessor name fix `request.state()` in `TaskService`).

## Files Touched (high level)
- src/main/java/.../service/TaskService.java
- src/main/java/.../service/TodolistService.java
- src/main/java/.../domain/entity/*.java (Task, Todolist, TaskComment, AuditLog, TodolistUser, UserSettings, Attachment, Category, AppUser, AppRole, GroupRole)
- src/main/java/.../web/dto/*.java (CreateTaskRequest, TaskResponse, UpdateTaskStatusRequest)
- src/test/java/.../service/TaskServiceTest.java
- src/main/java/.../security/SecurityConfig.java
- src/main/resources/application.yml

(Use Git history for exact file lists and commit SHAs.)

## Remaining / Recommended Next Steps
1. Run a full local build and tests: `mvn -DskipTests package` (or with tests enabled).
2. Create local Postgres DB (or use Docker) and load `supabase_schema.sql` for integration testing.
   - Docker example:
     ```bash
     docker run --name tulipani-db -e POSTGRES_PASSWORD=postgres -e POSTGRES_DB=tulipani -p 5432:5432 -d postgres:15
     docker cp supabase_schema.sql tulipani-db:/supabase_schema.sql
     docker exec -it tulipani-db psql -U postgres -d tulipani -f /supabase_schema.sql
     ```
3. Add negative unit tests (permission failures, duplicates, invalid inputs).
4. Review controllers to ensure they pass `authId` strings to service methods consistently.
5. Run integration tests against a real DB and fix any mapping/constraint issues.

## Notes
- Builds and runtime require a database; `application.yml` defaults to `jdbc:postgresql://localhost:5432/tulipani`.
- Many changes were conservative (relax nullability) to avoid breaking existing behavior; consider tightening constraints after DB testing.

---
Generated by Student2 work summary script.
