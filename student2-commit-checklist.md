# Student2 (Matej) — Commit checklist

Short linear commit messages to use while implementing business logic & ORM.

1. Add JPA entities and mappings
2. Add Spring Data JPA repositories
3. Implement TodolistService: createTodolist & listTodolists
4. Implement TaskService: createTask & getTask
5. Add business validation and duplicate checks
6. Add role checks and transaction management
7. Add AuditLog persistence for key actions
8. Add unit tests for TodolistService
9. Add unit tests for TaskService
10. Fix tests and minor service issues
11. Tidy mappings after review / adjust for DB schema

Usage:
- Work on one item at a time in branch `Student2-Matej`.
- Commit small, focused changes with the exact messages above.
- Push and open PR for review when a vertical (todolist/task) is complete.

Checklist status: update this file as you complete commits.
